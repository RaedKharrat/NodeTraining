// Créez maintenant une fonction nommée myFct qui :
// prend 2 arguments (arg1 et arg2)
// renvoie comme résultat : arg1*arg2 + arg1 + arg2

function myFct(arg1, arg2) {
    ena=arg1 * arg2 + arg1 + arg2;
    return ena;
  }
  
  const x = myFct(4,6);
  console.log(x);
  